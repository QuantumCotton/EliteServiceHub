export interface Theme {
  primary: string;
  secondary: string;
  accent: string;
  background: string;
  text: string;
  textMuted: string;
  primaryMuted: string;
  secondaryMuted: string;
  border: string;
}
